
// Since the table titles are how we query instead of IDs
// Created so that inital tables are populated. Handling new lower-level tables happens in the component
export enum SECTION_TITLE {
  SUMMARY = 'Summary',
  PROJECTS = 'Projects',
  WORKS = 'Work Experience',
  TOOLS = 'Tools',
  EDUCATION = 'Education'
}
